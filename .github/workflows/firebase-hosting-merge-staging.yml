# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting (Staging)
"on":
  push:
    branches:
      - dev
env:
  VITE_API_URL: ${{ vars.STAGING_VITE_API_URL }}
  VITE_STRIPE_PUBLIC_KEY: ${{ vars.STAGING_VITE_STRIPE_PUBLIC_KEY }}
permissions:
  contents: write
  issues: write
  pull-requests: write
  packages: write
  checks: write
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build:stage
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_GRAFT_STAGING }}"
          channelId: live
          projectId: graft-staging
          target: graft-v2
